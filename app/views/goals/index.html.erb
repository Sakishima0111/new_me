<h1>Goals</h1>
<!--<% @goals.each do |goal| %>-->
<!--  <%= link_to goal.user.nickname, user_path(goal.user.id) %>-->
<!--  <%= link_to goal.title, goal_path(goal.id) %>-->
<!--  <%= goal.content %>-->
<!--  <%= l goal.deadline %>-->
<!--  <%= goal.reward %>-->
<!--<% end %>-->

<div class="card-deck">
  <% @goals.each do |goal| %>
    <div class="card border-dark mb-3" style="max-width: 250px; min-width: 250px;">
      <div class="card-header"><%= link_to goal.user.nickname, user_path(goal.user.id), data: {"turbolinks" => false} %></div>
      <div class="card-body">
        <h5 class="card-title"><%=link_to goal.title, goal_path(goal.id) %></h5>
        <p class="card-text"><%= goal.content %><br>
          <%= render "cheers/btn", goal: goal %>
          <i class="fa-regular fa-message"></i> <%= goal.comments.count %>
        </p>
      </div>
    </div>
    
  <% end %>

</div>
