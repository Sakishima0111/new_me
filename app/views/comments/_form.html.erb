 <%= form_with model: [goal, comment], local: false do |f| %>
      <%= f.text_area :body, rows:'5', placeholder: "応援メッセージを送ろう！", class: "w-100 form-control", id: "comment_textarea" %>
      <%= button_tag type: "submit", class: "btn btn-primary text-right" do %>
        <i class="fa-solid fa-paper-plane text-light"></i>
      <% end %>
    <% end %>
