// 通報成功メッセージを表示
$('.success-message').text('コメントの通報が成功しました。');
setTimeout(function() {
  $(".success-message").fadeOut(100)
}, 1500)

// モーダルウィンドウを閉じる
$('#report-modal').addClass('fade-out');
setTimeout(() => {
  $('#report-modal').hide().removeClass('fade-out');
}, 300);