<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>

<h2>Profile</h2>
  
  <% if @user.image.attached? %>
    <%= image_tag @user.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'no_image', size: "200x200"  %>
  <% end %>
  
  <%= @user.nickname %>
  <%= @user.introduction %>
  
<%= link_to "プロフィール情報を編集", edit_user_path(current_user) %>
<% if current_user != @user %>
    <% if @user.followed_by?(current_user) %>
      <p><%=link_to "フォロー済み", user_relationships_path(tweet.user.id), method: :delete %></p>
    <% else %>
      <p><%=link_to "フォローする", user_relationships_path(tweet.user.id), method: :post %></p>
    <% end %>
<% end %>

<% @goals.each do |goal| %>
  <hr>
  <p><%=link_to goal.title, goal_path(goal.id) %></p>
  <p><%= goal.content %></p>
  <p><%= goal.deadline %></p>
<% end %>


